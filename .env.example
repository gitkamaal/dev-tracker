# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: This application now uses personal access tokens for authentication
# You don't need to set any OAuth credentials in environment variables
# Users will enter their tokens directly in the application 